import ProfileSettings from "@/components/profile/ProfileSettings";

const UserProfile = () => {
  return <ProfileSettings backPath="/my-submissions" deskTitle="Author Desk" allowPasswordChange homeLogoHref="/" />;
};

export default UserProfile;
