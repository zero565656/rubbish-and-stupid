import ProfileSettings from "@/components/profile/ProfileSettings";

const AdminProfile = () => {
  return <ProfileSettings backPath="/admin" deskTitle="Editorial Desk" />;
};

export default AdminProfile;
