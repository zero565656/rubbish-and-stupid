import ProfileSettings from "@/components/profile/ProfileSettings";

const EditorProfile = () => {
  return <ProfileSettings backPath="/editor" deskTitle="Editor Desk" allowPasswordChange homeLogoHref="/" />;
};

export default EditorProfile;
